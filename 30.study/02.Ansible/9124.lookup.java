lookup pluginはwith_*の処理、つまりloopを作るための処理が書かれたプラグイン

▼ 呼び出し方法
▼ lookup('プラグイン名', 引数)
-------------------------------------------------
- hosts: all
  tasks:
  - debug: msg="{{ lookup('csvfile', 'key1 col=2') }}"
-------------------------------------------------


▼ taskのアトリビュート1の形でwith_プラグイン名: 引数として呼び出す
-------------------------------------------------
- hosts: all
  tasks:
  - debug: msg="{{ item }}"
    with_items:
    - val1
    - val2
-------------------------------------------------
