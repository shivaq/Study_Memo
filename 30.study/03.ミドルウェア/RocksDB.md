RocksDB is an embeddable persistent key-value store for fast storage.

▼ High Performance
パフォーマンス最大化のために、a log structured database engine を使い、C++ で記述されている。

▼ Optimized for Fast Storage
RocksDB is optimized for fast, low latency storage such as flash drives and high-speed disk drives.
RocksDB exploits the full potential of high read/write rates offered by flash or RAM.

▼ Adaptable
RocksDB is adaptable to different workloads.
From database storage engines such as MyRocks to application data caching to embedded workloads,
RocksDB can be used for a variety of data needs.

▼ Basic and Advanced Database Operations
RocksDB provides basic operations such as opening and closing a database,
ßreading and writing to more advanced operations such as merging and compaction filters.
